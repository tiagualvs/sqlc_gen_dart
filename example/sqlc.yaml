version: "2"

plugins:
  - name: dart
    process:
      cmd: sqlc-gen-dart
      format: json

sql:
  - schema: "sqlite/schema.sql"
    queries: "sqlite/query.sql"
    engine: "sqlite"
    codegen:
      - plugin: dart
        out: lib/db
