version: "2"

plugins:
  - name: dart
    process:
      cmd: sqlc-gen-dart
      format: json

sql:
  - schema: "postgres/schema.sql"
    queries: "postgres/query.sql"
    engine: "postgresql"
    codegen:
      - plugin: dart
        out: lib/db
